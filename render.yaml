version: "1"
services:
  - type: web
    name: tct-0.0.0
    runtime: docker
    plan: free
    region: virginia
    dockerContext: .
    dockerfilePath: ./Dockerfile
    autoDeployTrigger: commit

    envVars:
      - key: SESSION_ID
        sync: true
      - key: TIMEZONE
        sync: true
      - key: PREFIX
        sync: true
      - key: POSTGRES_URL
        sync: true
